# 运行说明

> 这份文档回答一件事：**怎么把它跑起来、怎么停下来、出问题了怎么办。**
>
> 所有命令、输出、报错文本都取自实际运行记录（Windows 中文环境）。
> 想知道这个项目能干什么、有哪些用法，看 [`README.md`](README.md)。
>
> 本文档对应版本：`v1.3.0`（2026-09-24）

---

## 一、运行前需要知道的三件事

### 1.1 环境要求

| 项目 | 要求 | 说明 |
|---|---|---|
| 操作系统 | Windows | 两个入口脚本是 Windows 批处理；代码本身跨平台 |
| Python | 3.8 及以上 | 实测环境 3.13 |
| 依赖库 | 见 `requirements.txt` | requests / beautifulsoup4 / lxml / pandas / openpyxl（+ pytest 用于自测） |
| 网络 | 需可访问 `jy.xmu.edu.cn` | 抓取阶段联网；只筛选不抓取时**完全离线** |
| 磁盘 | 几百 KB | 数据库约 200 KB，导出的 Excel 每份约 40 KB |

### 1.2 两个入口脚本的分工

| 脚本 | 适用场景 |
|---|---|
| `一键运行.bat` | **双击就能跑**。抓取 → 打分 → 筛选 → 导出，一条龙。不带参数 |
| `run.bat` | **通用入口**，可带任意参数。日常筛选、看统计、跑测试都用它 |

`run.bat` 支持几个快捷写法（第一个参数生效）：

| 命令 | 作用 |
|---|---|
| `run.bat` | 完整流程：抓取 + 默认筛选 + 导出 |
| `run.bat xiamen` | 只用库中数据筛厦门岗位（不联网） |
| `run.bat --stats` | 看数据库概览 |
| `run.bat -h` | 列出全部参数 |
| `run.bat test` | 跑一遍自动化测试（116 个用例） |
| `run.bat spider` | 单独测试爬虫能不能连通 |

除这几个快捷词以外，第一个参数会被原样透传给 `main.py`，所以任何 `main.py` 的参数都能用：

```bat
run.bat --skip-crawl --city 厦门 --min-score 16 --level S,A
run.bat --search-city 福州 --search-salary-min 8000
```

### 1.3 解释器是怎么被找到的

这一步是理解后面所有报错的关键。`run.bat` 启动时按**顺序**探测，第一个命中的就用它：

```
1. .\local.bat 里设的 VENV_PY   ← 本机专属配置，不入版本库
2. .\.venv\Scripts\python.exe   ← 标准虚拟环境
3. .\venv\Scripts\python.exe    ← 另一种常见命名
4. 环境变量 %PYTHON%
5. PATH 里的 python
```

**为什么要设计成这样**：仓库不能写死某个人的解释器路径（别人 clone 下来会跑不通），但本机又必须用那个装好依赖的解释器。所以本机路径放在 `local.bat` 里——它被 `.gitignore` 忽略，永远不进版本库。

要指定本机解释器：

```bat
:: 1. 把模板复制一份
copy local.bat.example local.bat

:: 2. 编辑 local.bat，取消最后一行的注释，改成你的解释器路径
::    推荐：项目内的虚拟环境
set "VENV_PY=%~dp0.venv\Scripts\python.exe"
::    也可以用装在别处的解释器，写完整路径
:: set "VENV_PY=C:\Python313\python.exe"
```

删掉 `local.bat` 就退回自动探测。

---

## 二、安装

### 2.1 方式一：本机已配置好的解释器（最省事）

如果 `local.bat` 已经存在且指向一个装好依赖的解释器，**不用做任何安装**，直接跳到第三章。

### 2.2 方式二：自己建一个虚拟环境（标准做法）

```bash
# 在项目目录下执行
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

建在项目目录内（`.venv`）的好处是 `run.bat` 的自动探测会直接命中，不需要额外配置。

> PowerShell 里激活脚本时若报「禁止运行脚本」，先执行：
> `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`

### 2.3 方式三：让 `run.bat` 自己装

`run.bat` 会先做一次依赖检查，缺包就自动按 `requirements.txt` 安装。首次运行会看到：

```
[提示] 当前解释器缺少依赖，正在按 requirements.txt 安装...
       解释器：<探测到的解释器路径>
```

**注意两点**：

- 当前解释器**必须有 pip**。如果探测到的是一个没装 pip 的 Python，这一步会失败（见第五章坑 3）。
- 需要联网，装完才进入正式流程。

### 2.4 验证安装成功

最直接的验证是跑测试——不需要联网：

```bat
run.bat test
```

实测输出：

```
[运行全部单元测试]
........................................................................ [ 62%]
............................................                             [100%]
116 passed in 2.10s

请按任意键继续. . .
```

看到 `116 passed` 就说明环境完全正常。

---

## 三、启动

### 3.1 双击启动

在文件资源管理器里双击 **`一键运行.bat`**。窗口会打印解释器路径，然后直接开始抓取。

### 3.2 命令行启动

在项目目录打开命令行（在地址栏输入 `cmd` 回车即可），然后：

```bat
:: 完整流程，默认口径
run.bat

:: 只看数据库里现在有什么（秒级、不联网、不导出）
run.bat --stats

:: 只筛不抓（秒级）
run.bat --skip-crawl --city 厦门 --min-score 16 --level S,A
```

### 3.3 运行时会看到什么

抓取模式是七步流水线（前三步只发生在抓取模式），实测输出如下（`run.bat --max-pages 1`）：

```
==============================================================
  厦门在招岗位爬取与匹配筛选系统
  数据来源：厦门大学就业信息网 | 目标城市：厦门
  数据分层：ODS 贴源快照 -> 最新状态层 -> Excel 清单
  模式：增量抓取（只对新增/有变化的岗位重抓详情）
  检索条件：城市=厦门 | 学历=本科 | 性质=全职 | 发布=不限 | 专业=信息与计算科学/数学类/统计学
==============================================================
      抓取批次：20260921_110121（模式 incremental）

[1/7] 检索岗位（使用站点自带的多维筛选） ...
      列表页取到 20 条岗位（2 页，请求 1 次）

[2/7] 增量判定（对比 ODS 历史，决定哪些岗位需要重抓详情） ...
      ODS 中已记录 0 个岗位的历史
      需抓详情 20 条 / 免抓 0 条（共 20 条岗位，省下 0% 详情请求）
      新岗位=20

[3/7] 补详情页（需求专业 / 截止时间） ...
      详情请求 20 次（增量省下 0 次），取到专业字段 20 条

[4/7] 写入 ODS 贴源层（原样追加，不做业务加工） ...
      本批写入 20 条快照；ODS 累计 20 条 / 20 个岗位

[5/7] 刷新最新状态层（打分 + 按 job_key 更新） ...
      等级分布：S(极高匹配)=1 | A(高度匹配)=4 | B(一定匹配)=12 | C(相关性较弱)=3
      新增 20 条 | 更新 0 条（其中 0 条内容有变化）
      最新状态层累计 20 条（在架 20 条）

[6/7] 按条件筛选 ...
      条件：匹配分≥8 | 等级=S/A/B | 仅本科可投
      命中 17 条 / 库中共 20 条

[7/7] 导出 Excel ...
      已导出：...\output\岗位清单_厦门_SAB_min8_20260921_110200.xlsx

--------------------------------------------------------------
  筛选结果预览（前 10 条 / 共 17 条）
--------------------------------------------------------------
  [S]  27分  福建活石网络科技有限公司 - 数据分析/软件开发管培生
        城市：福建省厦门市湖里区 | 薪资：5000-10000 | 命中：方向:数据分析,计算机科学与技术,数据科学,计算机类,大数据
  [A]  24分  厦门微著智能装备有限公司 - CT重建算法工程师
        城市：福建省厦门市同安区 | 薪资：15000-30000 | 命中：方向:算法,应用数学,数学类
  ...
```

**同一条命令再跑一次**，[2/7] 和 [3/7] 会变成这样——这就是增量的效果：

```
[2/7] 增量判定（对比 ODS 历史，决定哪些岗位需要重抓详情） ...
      ODS 中已记录 20 个岗位的历史
      需抓详情 0 条 / 免抓 20 条（共 20 条岗位，省下 100% 详情请求）
      无需重抓=20

[3/7] 补详情页（需求专业 / 截止时间） ...
      详情请求 0 次（增量省下 20 次），取到专业字段 0 条
```

（`取到专业字段 0 条`不是出错——那 20 条的专业字段是从历史快照回填的，见 3.6。）

用 `--skip-crawl` 时是三步（跳过全部抓取环节）：

```
[1/3] 载入库中数据 ...
      最新状态层现有 20 条岗位（在架 20 条）
      ODS 贴源层累计 20 个岗位 / 40 条快照

[2/3] 按条件筛选 ...
      条件：匹配分≥14 | 仅本科可投
      命中 10 条 / 库中共 20 条
```

**日志和结果是分开的两条流**。上面这些是正常输出（stdout）；带时间戳的 `[INFO]` 行是日志（stderr），例如：

```
23:19:53 [INFO] spider.xmu_career: 第 1 页解析完成：共 2 页（每页 20 条）
23:19:54 [INFO] spider.xmu_career: 第 1/2 页：20 条（新增 20，累计 20）
23:19:57 [INFO] spider.xmu_career: 抓取完成：20 个岗位（列表请求 1 次，详情请求 2 次）
```

把输出重定向到文件时**只重定向 stdout 会丢掉日志**，排查问题时需要 `> 输出.txt 2>&1`。

### 3.4 怎么算「跑通了」

三个判定点，缺一不可：

1. `[1/7]` 那行显示**列表页取到的条数不是 0**（是 0 见 6.3）
2. 走完了 `[7/7] 导出 Excel`，并给出 `.xlsx` 路径
3. 打开那个文件，能看到按等级着色的岗位清单

### 3.5 耗时参考（实测）

| 场景 | 命令 | 耗时 |
|---|---|---|
| 只筛选，不抓取 | `--skip-crawl` | 1 秒内 |
| 看统计 | `--stats` | 1 秒内 |
| 快速试跑 | `--max-pages 1 --detail-limit 2` | 约 5 秒 |
| 爬虫自测 | `run.bat spider`（2 页 + 5 条详情） | 约 11 秒 |
| 默认口径全量（首次） | `run.bat`（2 页 + 约 24 条详情） | 约 1 分钟 |
| 默认口径全量（第二次起） | `run.bat` | **约 2 秒** |
| 放开专业全量（首次） | `--search-major 不限`（15 页 + 约 300 条详情） | 约 8-10 分钟 |
| 放开专业全量（第二次起） | `--search-major 不限` | **约 30 秒** |
| 从 ODS 重建（不联网） | `--rebuild-state` | 1 秒内 |

**耗时的大头是详情页**，不是列表页：列表每页约 1.7 秒，详情每条约 1.6 秒。300 条详情就要 8 分钟，这是为了拿「需求专业」付的代价——没有它匹配分就不可靠。

也正因为贵，才有增量：第二次及以后只抓「新增 / 有变化 / 缺专业 / 超期」的详情，其余跳过。详见 3.6。

---

### 3.6 增量运行：第二次为什么只要 1 秒

默认就带增量，不需要额外加参数。效果见 3.5 的表：20 条岗位的第二遍从 40 秒降到 1 秒。

原理一句话：**列表页照常全量翻，详情页只抓「该抓的」。**

判断规则（完整实现见 `core/incremental.py`，按优先级命中即停）：

| 情况 | 处理 |
|---|---|
| ODS 里没有这个职位 | 抓 —— 新岗位 |
| 列表字段（标题/薪资/城市/学历/发布日期）和上次不一样 | 抓 —— 信息有变 |
| 上次压根没抓到过「需求专业」 | 抓 —— 补上，否则那个字段永远是空的 |
| 距上次抓详情已超过 7 天 | 抓 —— 超期刷新，防「站点只改详情、不改列表」 |
| 以上都不是 | **跳过**，专业与截止时间从历史快照回填 |

**跳过不等于丢字段**。被跳过的岗位，`major_requirement` 会从 ODS 里**最近一次真正抓到详情的快照**取回来，导出的清单不会出现空白。这也是 ODS「只追加」的意义所在——历史抓到的事实都还在。

三个可以调的地方：

```bat
run.bat --full-refresh        :: 关掉增量，全部重抓详情（最慢但最全，排查用）
run.bat --detail-ttl 3        :: 把「超期刷新」的天数从 7 天改成 3 天
run.bat --no-detail           :: 一条详情都不抓（最快，但匹配分不准）
```

> **省的是重复请求，不是数据。** 列表页那十几次请求不会省——它是「有没有新岗位」的唯一来源，而且只要 20 多秒。增量省下的是那 94% 的详情页时间。

**什么时候该关掉增量**：怀疑某条岗位的信息没跟上（比如你手动去站点看了一眼，发现薪资变了而库里没变），先跑 `--ods-changes 7` 看有没有记录；确实没有就 `--full-refresh` 强制刷一遍。

---

## 四、停止与中断

### 4.1 正常结束

- **命令行方式**：跑完自动退出，退出码 0。
- **双击 bat 方式**：跑完后会停在 `请按任意键继续. . .`，这是 `pause` 在等你——**不是卡住**。按任意键关闭窗口。

### 4.2 中途取消

按 `Ctrl+C`。但要知道代价：

> **抓取过程中按 Ctrl+C，这一轮抓到的数据会全部丢弃。**

原因在流程设计上：程序是「先把这一轮全部抓完，再统一写盘」（见 main.py 的 `run_pipeline()`：检索 → 增量判定 → 补详情 → 写 ODS → 刷新状态层）。在补详情阶段按 `Ctrl+C`，还没走到「写 ODS」那一步，所以这一轮抓到的会丢。

**已经写进 ODS 的历史不受影响**，而且下次跑会更省：增量判定会发现哪些岗位「这次没抓成功」，下一轮只补那几条，不会从零重来。

想少付这个代价，就用 `--max-pages` / `--detail-limit` 先小规模试跑。

### 4.3 这个项目没有常驻进程

不存在「服务」或「后台任务」需要关停。窗口关掉就等于彻底停下，不会残留任何进程。

**但有一种情况看起来像卡死**：单次请求最长等 15 秒超时，失败后按 1、2、4 秒退避重试最多 3 次。最坏情况下一条请求要 60 秒以上才有反馈，此时光标不动、界面无输出。判断方法——等一分钟，或者另开一个窗口看网络是否在动；如果多条都这样，就是网络问题（见 6.3）。

---

## 五、实际踩过的坑

下面每条都是真实发生过的，按「现象 → 根因 → 处置」写，几个不容易直接判断的坑补上了**怎么一眼认出它**。代价最大、最容易被误判成「项目崩了」的两条标了 ★。

### ★ 坑 1：`.bat` 被编辑器另存后，满屏「不是内部或外部命令」

**现象**。窗口刷出一屏这样的内容，看起来像整个项目崩了：

```
[杩愯�屽叏閮ㄥ崟鍏冩祴璇昡
'generic' is not recognized as an internal or external command,
operable program or batch file.
'ilter' is not recognized as an internal or external command,
'quirements.txt' is not recognized as an internal or external command,
```

中文 Windows 上第一行是乱码，后面是「'xxx' 不是内部或外部命令，也不是可运行的程序或批处理文件。」

**判断**。看第一行——**只要第一行中文是乱码，就不是 Python 的问题，是 `.bat` 文件本身被读坏了**。真正的 Python 报错不会带乱码。

**根因**。三个问题叠加：

| 问题 | 后果 |
|---|---|
| 文件存成了 UTF-8，而 cmd.exe 按 GBK(936) 解码 | 中文注释和提示变成乱码 |
| 乱码字节里含 cmd 特殊字符（`\|` `&` `>`） | 一行命令被从中间拆断，变成两条垃圾命令 |
| 换行是 LF 而不是 CRLF | `if (...)` 这类多行结构解析失效 |

实测复现数据：把一个正常的 `run.bat` 转成 UTF-8 + LF 后运行 → 文件里 **0 个 CRLF、84 个纯 LF**，cmd 报出 **12 条「不是内部或外部命令」+ 2 条「找不到指定路径」**。

**触发条件**——以下任何一条都会让格式变坏：

- 用 VS Code / 记事本 / Notepad++ 打开这两个 bat 并保存
- 用脚本或工具重新生成 bat 文件（**包括 AI 工具直接写文件**）

**处置**。**首选：不要编辑这两个 bat**。要改参数就把参数跟在命令后面，不改文件。

非改不可时，改完转回 GBK + CRLF。在项目目录下打开 PowerShell 执行：

```powershell
$p = (Resolve-Path "run.bat").Path
$t = [System.IO.File]::ReadAllText($p, [System.Text.Encoding]::UTF8) -replace "`r?`n", "`r`n"
[System.IO.File]::WriteAllText($p, $t, [System.Text.Encoding]::GetEncoding(936))
```

**这条命令里有两个必须照抄的细节**（都是实测踩出来的）：

- **必须先转成绝对路径**。`[System.IO.File]` 这类 .NET 方法的相对路径基准是**进程启动目录**，不是 PowerShell 的当前目录——`Set-Location` 不会同步它。直接写 `ReadAllText("run.bat")` 很可能读不到文件，而错误只进 stderr，容易被忽略，最后看到的现象是「命令跑了但文件一点没变」。所以用 `(Resolve-Path "run.bat").Path` 先拿到绝对路径。
- **编码用 `936`**，不要用 `GB2312`。936 就是 GBK，是 GB2312 的超集，能覆盖更多字符。

实测这条命令的效果：把一个 UTF-8 + LF 的 `run.bat` 转回后，**字节数与原始文件完全一致**（2632 字节 / 84 个 CRLF / 0 个纯 LF）。

**自查命令**（确认文件的换行格式对不对）：

```powershell
$p = (Resolve-Path "run.bat").Path
$t = [System.Text.Encoding]::ASCII.GetString([System.IO.File]::ReadAllBytes($p))
"CRLF={0}  LF总={1}" -f ([regex]::Matches($t, "`r`n")).Count, ([regex]::Matches($t, "`n")).Count
```

实测两种结果，差别一目了然：

```
正常 run.bat  ->  CRLF=84  LF总=84     ← 两者相等 = 全是 CRLF，正常
损坏 _bad.bat ->  CRLF=0   LF总=84     ← 全是 LF，被改过了
```

另外仓库里配了 `.gitattributes` 强制 `*.bat` 用 CRLF，所以**从 git 重新 checkout 一次也能修复**（前提是本地改动不要了）：

```bat
git checkout -- run.bat
```

> 为什么不在 bat 开头写 `chcp 65001` 改用 UTF-8？实测在中文 Windows 下不可靠：`chcp` 只影响之后的输出，对 cmd 解析文件本身帮助有限，反而让中文提示两侧编码不一致。现在的做法是「文件 GBK + 不切代码页 + `set PYTHONIOENCODING=gbk:replace`」——bat 里的中文正常，Python 输出的中文也正常（控制台本来就是 936），`replace` 保证遇到 GBK 表示不了的字符时只显示成 `?`，不会让程序崩掉。

### ★ 坑 2：直接用 `python main.py` 报缺依赖

**现象**：

```
==============================================================
  [启动失败] 当前 Python 环境缺少运行依赖
==============================================================
  正在使用的解释器：D:\Python313\python.exe

  缺少以下模块：
    - lxml       pip 包名 lxml             HTML 解析后端
    - pandas     pip 包名 pandas           数据处理与导出
    - openpyxl   pip 包名 openpyxl         Excel 读写引擎

  【推荐】用项目自带的虚拟环境（依赖已装好，无需配置）
      在本目录下执行：
      run.bat --skip-crawl --city 厦门 --min-score 16 --level S,A

  【注意】当前解释器没有安装 pip，无法自行补装依赖，
          请直接使用上面推荐的 run.bat。
==============================================================
```

**判断**。这不是崩溃，是程序启动时的自检拦截——注意它明确告诉了你三件事：**哪个解释器在跑、缺什么、怎么修**。相比一段裸的 `ModuleNotFoundError` traceback，信息量完全不同。

**根因**。机器上存在多个 Python，而 `python` 这个命令指向的那个没装本项目依赖。

**处置**。用 `run.bat`（它会自动找到正确的解释器）。要确认当前 `python` 指向谁：

```bat
python -c "import sys; print(sys.executable)"
```

**顺带一个设计细节**：自检还会检测解释器有没有 pip。没有 pip 时它**不会**给出 `pip install` 建议——因为那是一条注定失败的命令，不如不给。这个判断逻辑在 `main.py` 的 `check_dependencies()` 里。

### 坑 3：`python -m pip install` 报 `No module named pip`

**现象**：

```
D:\Python313\python.exe: No module named pip
```

**根因**。这个解释器是精简安装，**根本没带 pip**。

**处置**。`pip install` 这条路走不通，别在这上面耗时间。换一个带 pip 的解释器，或者用第 1.3 节的方式通过 `local.bat` 指定。

### 坑 4：改了 `config.py` 的权重表，分数却一点没变

**现象**。改完 `KEYWORD_WEIGHTS` / `DIRECTION_WEIGHTS` 再跑，库里岗位的分数和等级完全照旧。

**根因**。入库用的是 `INSERT OR IGNORE`（按「公司 + 岗位」去重）。重跑时这些岗位已存在，整条记录被跳过，**包括分数字段**。

**处置**。用专门的重新打分入口，不联网、秒级完成：

```bat
run.bat --rescore
```

这是「数据与算法解耦」的直接收益：原始数据不动，只重算派生字段。改打分规则再也不用重抓 8 分钟。

### ★ 坑 5：`--filter-file` 指向不存在的文件，程序不报错、直接用默认条件

**现象**。实测 `run.bat --skip-crawl --filter-file build/不存在的方案.json`：

```
[2/3] 按条件筛选 ...
      条件：仅本科可投
      命中 284 条 / 库中共 284 条
```

**没有报错**，退出码是 0，但「条件：」那行显示的是**默认条件**，不是你文件里的条件。

**根因**。方案文件不存在时，载入逻辑静默退回默认值。这是「静默失败」——比报错更危险，因为它让你以为条件生效了。

**处置**：

1. **养成一个习惯——筛完看一眼「条件：」那一行**，确认列出来的正是你要的条件。这是唯一能发现「静默失败」的地方。
2. 确认文件真的存在：`dir 你的方案文件.json`。
3. 路径基准是**项目目录**，不是你的当前目录——因为 `run.bat` 内部会先切到项目目录再执行。所以 `--filter-file focus.json` 指的是项目目录下的 `focus.json`。

### 坑 6：抓详情页很慢，看起来像卡住

**现象**。列表很快就出来了，然后长时间没有输出，光标不动。

**根因**。详情页是**逐条**抓的，每条之间强制间隔 1.6 秒（对站点的礼貌节流，也是合规要求）。300 条约 8 分钟，期间只在每 5 条打一行 `详情进度 5/5`。

**判断**。看日志里有没有 `详情进度` 在递增：

```
23:17:39 [INFO] spider.xmu_career: 开始抓取详情页补「需求专业」，共 5 个岗位，预计约 0.1 分钟
23:17:48 [INFO] spider.xmu_career:   详情进度 5/5（已取到专业 5 条）
```

**处置**。想快就用下面两个之一，代价写清楚了：

| 参数 | 效果 | 代价 |
|---|---|---|
| `--detail-limit 10` | 只补前 10 条详情 | 其余条目的专业字段为空，分数偏低 |
| `--no-detail` | 完全不抓详情 | **所有**岗位都缺「需求专业」，匹配分只能当参考 |

用 `--no-detail` 时程序会显式提醒：

```
  ★ 已关闭详情页抓取：缺少「需求专业」，匹配分仅供参考
```

### 坑 7：第二次运行，显示「新增 0 条，更新 20 条」

**现象**。同一个目标跑第二遍，第五步变成：

```
[5/7] 刷新最新状态层（打分 + 按 job_key 更新） ...
      新增 0 条 | 更新 20 条（其中 0 条内容有变化）
      最新状态层累计 20 条（在架 20 条）
```

**这是正常的，而且是设计目标**。「新增 0」= 这一批没有新出现的岗位；「更新 20」= 已有记录按这次抓到的内容刷新了一遍。同时 `[3/7]` 会显示「详情请求 0 次」——因为有 20 条岗位判定为无变化，直接跳过了详情页。

**和改造前的区别（重要）**：
改造前用 `INSERT OR IGNORE`，重复记录**整条跳过**，所以岗位后来改了薪资、改了截止时间，库里留着的还是第一次抓到的旧值。
现在改成按 `job_key` 更新，信息会跟着刷新，并记下「相对上次变过几次」（`change_count`）。
去重键也从「公司 + 岗位名」换成了站点的**职位 ID**——公司把标题改一个字（比如加个「（急招）」）**不会**再入库成两条。这是老版本坑 7 里明确写过的坑，已经修掉。

**处置**：

```bat
:: 看最近 30 天有哪些岗位信息变了（不联网）
run.bat --ods-changes 30

:: 看单个岗位的完整轨迹：哪次抓的、当时什么值、哪次开始变的
run.bat --ods-history 2400730

:: 怀疑数据没跟上，强制把全部详情重抓一遍
run.bat --full-refresh

:: 只想取最近发布的岗位
run.bat --search-time 近3天
```

### 坑 8：输出的 Excel 文件名每次都不一样，找不到最新那份

**现象**。`output/` 目录里堆了十几份 `岗位清单_*.xlsx`。

**这不是 bug，是刻意设计**。文件名带上了筛选条件标签，所以：

- `岗位清单_厦门_min16_xxx.xlsx` = 厦门 + 匹配分≥16 的那一轮
- `岗位清单_厦门_SA_min16_xxx.xlsx` = 厦门 + S/A 级 + 匹配分≥16
- 末尾 `20260920_231957` 是运行时刻

如果不加条件标签，两次不同条件的运行会互相覆盖，你就分不清手里这份是哪套条件跑出来的。

**处置**。想指定文件名用 `--out 我的清单.xlsx`。要清理历史产物直接删 `output/` 里的 xlsx 即可（见第七章）。

---

## 六、报错对照

### 6.1 速查表

| 现象 | 大概率原因 | 直接处置 |
|---|---|---|
| 满屏 `'xxx' 不是内部或外部命令` + 中文乱码 | `.bat` 被存成了 UTF-8 / LF | 见坑 1；`git checkout -- run.bat` 可一键复原 |
| `[启动失败] 当前 Python 环境缺少运行依赖` | 用错了 Python | 改用 `run.bat`；见坑 2 |
| `No module named pip` | 解释器没带 pip | 换解释器；见坑 3 |
| `ModuleNotFoundError: No module named 'core'` / `'config'` | 直接运行了 `core/` 下的模块文件 | 回项目根目录，从 `main.py` / `run.bat` 进入 |
| `No such file or directory: 'main.py'` | 没在项目目录下执行 | 先 `cd` 到项目目录 |
| `'python' 不是内部或外部命令` | Python 没进 PATH | 用 `local.bat` 指定完整路径（见 1.3） |
| `无法加载文件 Activate.ps1，因为在此系统上禁止运行脚本` | PowerShell 执行策略 | `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` |
| `PermissionError` 导出失败 | 目标 Excel 正被打开 | 关掉 Excel 再运行 |
| 取到 0 条岗位 | 检索条件拼写错误 / 网络不通 | 见 6.3 |
| 筛选结果 0 条 | 筛选条件太严 | 见 6.4 |
| 分数改动没生效 | 已有记录不会被整条覆盖 | `run.bat --rescore`；见坑 4 |
| 条件看起来没生效 | 方案文件没载入成功（静默失败） | 看「条件：」那行；见坑 5 |

### 6.2 启动类：程序根本没跑起来

**怎么定位是「环境」还是「程序」问题**：先跑 `run.bat test`。

- 测试通过（116 passed）→ 环境没问题，看 6.3
- 测试也报错 → 环境问题，回到坑 1 / 坑 2 / 坑 3

**空数据库是正常的**。全新 clone 后直接 `run.bat --stats`，会看到：

```
数据库为空，请先运行：python main.py
```

此时 `--skip-crawl` 也走不通（没有数据可筛）：

```
[1/3] 载入库中数据 ...
      库中现有 0 条岗位
      库为空。请先运行一次抓取：python main.py
```

`--rescore` 同理：

```
数据库为空，无需重打分。请先运行：python main.py
```

**处置**：先跑一次 `run.bat` 抓数据。三条提示都明确告诉你该做什么，没有静默跳过。

### 6.3 抓取类：一条都没取到

实测把检索城市填成一个不存在的名字，完整输出是：

```
[1/5] 检索岗位（使用站点自带的多维筛选） ...
      检索条件：城市=火星 | 学历=本科 | 性质=全职 | 发布=不限 | 专业=信息与计算科学/数学类/统计学
      取到 0 条岗位（列表请求 1 次，详情请求 0 次）
      其中取到「需求专业」的 0 条
      未取到任何岗位，流程结束。
      排查建议：确认检索条件拼写（城市/学历/性质用中文名），或先跑 python -m spiders.xmu_career 自测。
```

日志里同时会有一条 WARNING：

```
[WARNING] spider.xmu_career: 检索结果为空，请检查检索条件是否正确
```

**按这个顺序排查**：

1. **检索条件拼写**。`--search-city` / `--search-education` / `--search-category` 都填**中文名**（`厦门` `本科` `全职`），不要填代码，也不要用简称。可用取值直接跑 `run.bat -h` 看帮助里的选项列表。
2. **确认爬虫本身能连通**：
   ```bat
   run.bat spider
   ```
   这个自测不依赖数据库和筛选逻辑，直接打站点。能拿到数据 → 说明网络和页面结构都正常，问题在你的检索条件；也拿不到 → 看第 3 步。
3. **网络是否可达** `jy.xmu.edu.cn`。抓取失败时日志里会有 `请求失败 <url> -> <原因>`，比如连接超时、DNS 失败。
4. **确认没把条件设成矛盾组合**。例如城市和学历都对，但发布时间选了「近1天」而当天没有新岗位，也会是 0 条。

### 6.4 筛选类：抓到了数据，但筛出来是 0 条

实测 `run.bat --skip-crawl --min-score 999`：

```
[2/3] 按条件筛选 ...
      条件：匹配分≥999 | 仅本科可投
      命中 0 条 / 库中共 284 条

      筛选结果为 0 条，已跳过导出。
      可尝试放宽条件：调低 --min-score、去掉 --city，或用 python main.py --stats 看看库里实际有哪些数据。
```

**注意程序的处理方式：跳过导出，而不是给你一个空 Excel。** 空表比报错更容易误导人。

**处置顺序**：

1. `run.bat --stats` 看库里实际有什么（等级分布、城市分布、Top 10）
2. 对照等级分档放宽：`--min-score 16` → `--min-score 8` → 去掉
3. 去掉 `--city`（城市是硬筛选，会把外地岗位全丢掉）

**另外，被城市筛掉多少条程序会明确告诉你**：

```
  已按城市筛选，另有 N 条非目标城市岗位未列入（去掉 --city 可一并看到）
```

看到这行就别以为库里数据少——只是因为筛了城市。

**B 级和 C 级多是正常的**。库里的岗位来自整个厦门本科招聘市场（教师、销售、行政、制造、医药都有），它们落在 C 级是正常结果。判断标准是「**有没有筛出对口岗位**」，不是「C 级多不多」。

---

## 七、运行会产生的文件

### 7.1 文件清单

| 路径 | 什么时候产生 | 说明 |
|---|---|---|
| `data/jobs.db` | 首次运行自动创建 | SQLite 岗位库。**这是核心数据**，重跑不会丢 |
| `output/*.xlsx` | 每次导出 | 筛选结果，按条件命名 |
| `_spider_out.txt` | 跑 `run.bat spider` 或爬虫自测时 | 自测结果明细，写在项目根目录 |
| `__pycache__/`、`.pytest_cache/` | 运行 / 跑测试时 | Python 字节码缓存 |
| `local.bat` | 手工创建 | 本机解释器路径，不提交 |
| `*.json` | 用 `--save-search` / `--save-filter` 时 | 条件方案文件，写在项目目录下 |

`data/` 和 `output/` 目录由 `config.py` 在导入时自动创建，**不需要手动建**。全新 clone 下来直接跑就行。

### 7.2 哪些可以放心删

| 可以删 | 影响 |
|---|---|
| `output/` 里的 xlsx | 无。重新筛一次就有 |
| `_spider_out.txt` | 无。下次跑自测会重新生成 |
| `__pycache__/`、`.pytest_cache/` | 无。缓存而已 |
| `data/jobs.db` | **要重抓 8 分钟**。想清空重来建议用 `run.bat --reset-db` 而不是直接删文件 |

`local.bat` 删了也能跑（会退回自动探测），只是可能探测到依赖不全的解释器。

---

## 八、命令速查

```bat
:: ---- 环境 ----
run.bat test                          :: 跑 161 个测试，验证环境
run.bat spider                        :: 单独测试爬虫连通性
run.bat -h                            :: 全部参数

:: ---- 抓取 ----
run.bat                               :: 默认口径（厦门+本科+全职+数学统计类）
run.bat --search-major 不限           :: 放开专业，取厦门全部本科全职岗位
run.bat --search-time 近1周           :: 只取近一周发布的
run.bat --search-city 福州            :: 换城市
run.bat --max-pages 2 --detail-limit 10   :: 快速试跑
run.bat --no-detail                   :: 不抓详情（快，但分数不准）
run.bat --reset-db --city 厦门        :: 清库后重抓

:: ---- 筛选（不联网，秒级）----
run.bat --stats                       :: 看库里有什么
run.bat --skip-crawl --city 厦门      :: 只筛厦门
run.bat --skip-crawl --min-score 16 --level S,A
run.bat --skip-crawl --exclude 销售,客服
run.bat --skip-crawl --top 20 --out 今日清单.xlsx

:: ---- ODS 贴源层 / 增量 ----
run.bat --ods-stats                   :: 历次抓取批次跑了什么、省了多少请求
run.bat --ods-history 2400730         :: 某个岗位的完整历史轨迹
run.bat --ods-changes 30              :: 最近 30 天哪些岗位信息变了
run.bat --rebuild-state               :: 从原始快照重建（不联网，1 秒）
run.bat --full-refresh                :: 关掉增量，全部重抓详情
run.bat --detail-ttl 3                :: 详情字段的可信天数（默认 7）
run.bat --active-only                 :: 排除标记为失效的岗位
run.bat --reset-db                    :: 清最新状态层并从 ODS 自动重建
run.bat --reset-ods --reset-db        :: 连历史一起清掉（不可恢复）

:: ---- 打分规则改了之后 ----
run.bat --rescore                     :: 按新规则重算，不联网

:: ---- 方案文件 ----
run.bat --city 厦门 --min-score 16 --level S,A --save-filter focus.json
run.bat --skip-crawl --filter-file focus.json
```

完整的参数说明见 `README.md` 第四章。

---

## 附：合规约定

这是项目能长期跑下去的前提，改动代码时不要破坏：

- 只抓公开可访问页面，**不登录、不绕过任何访问控制**
- 每次请求间隔不低于 1.5 秒（`config.REQUEST_DELAY_SECONDS`），详情页 1.6 秒
- 失败只做有限次退避重试（最多 3 次），不做高频重试
- 抓取结果仅供个人求职使用，不再发布、不商用

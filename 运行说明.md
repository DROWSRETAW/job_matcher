# 运行说明（针对本机环境）

> 项目核心链路：**爬取厦门大学就业信息网 → 打分 → 全量入库 → 按用户条件筛选 → 导出 Excel**
>
> 本机已配好 Python 虚拟环境，**依赖全部安装完毕，可以直接运行**。
>
> ⚠️ **唯一要记住的一条规则：在本目录下一律用 `run.bat`，不要敲 `python main.py`。**
> 因为本机的 `python` 指向系统 Python（`D:\Python313`），它**既没装依赖、也没有 pip**。
> 原因和自检方法见下方《常见报错》。

---

## ⚡ 最快上手：双击运行

项目目录下有两个批处理脚本，**双击就能跑，不用记任何路径**：

| 脚本 | 作用 |
|---|---|
| **`一键运行.bat`** | 爬取厦大就业网 → 按默认条件筛选 → 导出 Excel |
| **`run.bat`** | 通用入口，可带参数（见下表） |

> ⚠️ 这两个 bat 是 **GBK + CRLF** 格式（Windows 原生）。**不要用编辑器打开再保存** ——
> VS Code / 记事本都会把它转成 UTF-8，脚本会立刻失效（症状见《报错：刷出一屏…》）。
> 要换参数就别改文件，直接把参数跟在命令后面。

**`run.bat` 支持的用法**：

| 命令 | 作用 |
|---|---|
| `run.bat` | 爬取 + 默认筛选 + 导出 |
| `run.bat xiamen` | **只筛厦门岗位**（用库中数据，秒级，不联网） |
| `run.bat --skip-crawl --city 厦门,福州 --min-score 16 --level S,A` | 自定义条件重筛 |
| `run.bat --stats` | 查看数据库概览（等级分布 / 城市分布 / Top10） |
| `run.bat -h` | 查看全部筛选参数 |
| `run.bat test` | 运行单元测试（54 个用例） |
| `run.bat spider` | 单独测试厦大就业网爬虫 |

> 💡 双击 `.bat` 无法带参数时，在项目目录开一个 CMD 窗口，敲 `run.bat xiamen`。

---

## ⚠️ 常见报错：`ModuleNotFoundError: No module named 'pandas'`

**这个报错的唯一原因：用了系统 Python，而不是项目虚拟环境。**

```
Traceback (most recent call last):
  File "...\core\exporter.py", line 10, in <module>
    import pandas as pd
ModuleNotFoundError: No module named 'pandas'
```

### 为什么会这样

本机存在两个 Python，`python` 命令默认走的是第一个：

| 解释器 | 路径 | pandas/openpyxl/lxml | pip | 结果 |
|---|---|---|---|---|
| **系统 Python**（`python` 默认指向） | `D:\Python313\python.exe` | ❌ 全都没有 | ❌ **连 pip 都没有** | 报错 |
| **项目虚拟环境** | `...\binaries\python\envs\default\Scripts\python.exe` | ✅ 全部齐全 | ✅ pip 26.1.2 | 正常 |

> 注意第二列最后一项：系统 Python **没有 pip**，所以 `python -m pip install ...` 这条常规修法
> 在你机器上是行不通的（会报 `No module named pip`）。别在这上面浪费时间。

### 解决办法

**直接在项目目录下用 `run.bat`**，它内部已经写死了虚拟环境路径，参数原样透传：

```bat
run.bat --skip-crawl --city 厦门 --min-score 16 --level S,A
```

如果你更想敲 `python`，就把虚拟环境激活，激活后 `python` 才会指向正确解释器：

```bat
.venv\Scripts\activate
python main.py --skip-crawl --city 厦门
```

### 程序自带自检（v2 新增）

从这一版起，`main.py` 在**任何第三方库导入之前**会先做依赖自检。缺包时不再抛裸 traceback，
而是直接告诉你三件事：**当前用的是哪个解释器、缺哪些包、该怎么修**。实测输出：

```
==============================================================
  [启动失败] 当前 Python 环境缺少运行依赖
==============================================================
  正在使用的解释器：D:\Python313\python.exe

  缺少以下模块：
    - lxml       pip 包名 lxml             HTML 解析后端
    - pandas     pip 包名 pandas           数据处理与导出
    - openpyxl   pip 包名 openpyxl         Excel 读写引擎

  【推荐】用项目自带的虚拟环境（依赖已装好，无需配置）
      在本目录下执行：
      run.bat --skip-crawl --city 厦门 --min-score 16 --level S,A

  【注意】当前解释器没有安装 pip，无法自行补装依赖，
          请直接使用上面推荐的 run.bat。
==============================================================
```

自检还会**自动检测 pip 是否存在**：若当前解释器没有 pip，它就不显示那条注定失败的
`pip install` 命令，避免你照抄一次再踩一个坑。

**随时自查当前解释器**：

```bat
python -c "import sys; print(sys.executable)"
```

看到路径里含 `envs\default` 才算对。

---

## ⚠️ 报错：刷出一屏 `'xxx' 不是内部或外部命令`（且中文是乱码）

完整症状长这样：

```
'en' 不是内部或外部命令，也不是可运行的程序或批处理文件。
'分桶蛊例级===' 不是内部或外部命令，也不是可运行的程序或批处理文件。
'.st_core.py' 不是内部或外部命令，也不是可运行的程序或批处理文件。
'se' 不是内部或外部命令，也不是可运行的程序或批处理文件。
```

**这不是 Python 报错，是 `.bat` 文件本身被 cmd 读坏了。**

### 原因

`.bat` 必须保存为 **GBK 编码 + CRLF 换行**（Windows 原生格式）。出问题时文件变成了
**UTF-8 编码 + LF 换行**，于是：

| 问题 | 后果 |
|---|---|
| 文件是 UTF-8，cmd.exe 按 GBK(936) 解码 | 中文注释/提示变成乱码 |
| 乱码字节里恰好含 cmd 特殊字符（`\|` `&` `>`）| 命令行被从中间拆断 |
| 换行是 LF 而非 CRLF | cmd 的多行结构（`if (...)`）解析失效 |

三者叠加，就是每一行都被当成一条独立命令去执行 → 满屏「不是内部或外部命令」。

### 什么时候会触发

- **用编辑器打开这两个 bat 并保存**（VS Code / 记事本 / Sublime / Notepad++ 都会默认存成 UTF-8）
- 用工具重新生成 bat 文件（包括 AI 工具直接写文件）

### 怎么避免

**① 不要编辑这两个 bat 文件。** 要改参数就把参数跟在命令后面，不改文件：

```bat
run.bat --skip-crawl --city 厦门 --min-score 16 --level S,A
```

**② 如果你非改不可**，改完必须转回 GBK + CRLF：

```powershell
$p = "run.bat"
$t = [System.IO.File]::ReadAllText($p, [System.Text.Encoding]::UTF8) -replace "`r?`n", "`r`n"
[System.IO.File]::WriteAllText($p, $t, [System.Text.Encoding]::GetEncoding('GB2312'))
```

**③ 或者让我重新生成一份。**

### bat 里面为什么不写 `chcp 65001`

早期版本试过「UTF-8 文件 + 开头 `chcp 65001`」。实测在中文 Windows 下**不可靠**：
`chcp` 只影响后续输出，对 cmd 解析文件本身帮助有限，反而让中文提示两侧编码不一致。

现在的做法是：**文件 GBK + 不切代码页 + `set PYTHONIOENCODING=gbk:replace`**。
这样 bat 里的中文提示正常，Python 输出的中文也正常（控制台本来就是 936），
`replace` 保证万一遇到 GBK 表示不了的特殊字符也只是显示成 `?`，不会让程序崩掉。

---

## 一、环境准备

| 项目 | 要求 |
|---|---|
| Python | 3.10+（开发环境为 3.13） |
| 依赖 | `requests` / `beautifulsoup4` / `lxml` / `pandas` / `openpyxl` / `pytest` |
| 虚拟环境 | 推荐建在项目目录内（`.venv`），也可用本机任一装好依赖的解释器 |

建议按 Python 项目的通用做法，在项目目录内建一个独立环境：

```bash
python -m venv .venv                  # 创建
.venv\Scripts\activate                # 激活（Windows）
pip install -r requirements.txt       # 安装依赖
```

**下文用 `$py` 代指 Python 解释器**。激活虚拟环境后它就是 `.venv\Scripts\python.exe`；
也可以不激活、直接写完整路径调用。项目里的 `run.bat` 会自动按
`.venv` → `venv` → `%PYTHON%` → `PATH` 的顺序寻找解释器，无需额外配置。

---

## 二、跑起来

### 第 1 步：进入项目目录

```bash
cd <项目目录>
```

### 第 2 步：运行主流程

```bash
$py main.py --city 厦门
```

**实测输出**（`--city 厦门 --max-pages 1 --detail-limit 2`）：

```
==============================================================
  厦门在招岗位爬取与匹配筛选系统
  数据来源：厦门大学就业信息网 | 目标城市：厦门
  检索条件：城市=厦门 | 学历=本科 | 性质=全职 | 发布=不限 | 专业=信息与计算科学/数学类/统计学
==============================================================

[1/5] 检索岗位（使用站点自带的多维筛选） ...
      取到 20 条岗位（列表请求 1 次，详情请求 2 次）
      其中取到「需求专业」的 2 条

[2/5] 专业匹配度打分 ...
      等级分布：S(极高匹配)=0 | A(高度匹配)=0 | B(一定匹配)=4 | C(相关性较弱)=16

[3/5] 入库（按 公司+岗位 去重） ...
      新增 0 条，重复跳过 20 条
      库中累计 284 条（入库为全集，筛选只影响导出结果）

[4/5] 按条件筛选 ...
      条件：城市=厦门 | 仅本科可投
      命中 284 条 / 库中共 284 条

[5/5] 导出 Excel ...
      已导出：output/岗位清单_厦门_20260920_223511.xlsx

--------------------------------------------------------------
  筛选结果预览（前 10 条 / 共 284 条）
--------------------------------------------------------------
  [S]  27分  福建活石网络科技有限公司 - 数据分析/软件开发管培生
        城市：福建省厦门市湖里区 | 薪资：5000-10000 | 命中：方向:数据分析,计算机科学与技术,数据科学,计算机类,大数据
  [A]  24分  厦门微著智能装备有限公司 - CT重建算法工程师
        城市：福建省厦门市同安区 | 薪资：15000-30000 | 命中：方向:算法,应用数学,数学类
  [A]  20分  厦门微著智能装备有限公司 - 算法工程师
  [A]  20分  厦门海景微电子有限公司 - AI开发工程师
  [A]  19分  厦门瑞为信息技术有限公司 - 产品工程师（软件方向）
  [A]  19分  厦门金美信消费金融有限责任公司 - 开发工程师
  [A]  18分  中国建设银行股份有限公司厦门市分行 - 科技类专项人才
  [A]  18分  厦门佳芯电子有限公司 - 固件工程师（嵌入式软件方向）
  [A]  18分  厦门海景微电子有限公司 - C++开发工程师
  [A]  18分  厦门狄耐克智能科技股份有限公司 - 安卓开发工程师
--------------------------------------------------------------

  结果城市分布：福建省厦门市思明区=93 | 福建省厦门市湖里区=55 | 福建省厦门市=51
```

看到这个，说明**全链路跑通了**。

### 第 3 步：打开导出的 Excel

```
output\
```

里面的 `岗位清单_xxx.xlsx` 就是筛选结果：按匹配度排序，S 级标浅红、A 级标浅橙、B 级标浅黄。
文件名会带上你这次用的条件（如 `岗位清单_厦门_SA_min16_...xlsx`），所以连着试几套条件不会互相覆盖。

---

## 二·补 · 筛选条件怎么用（本项目重点）

**所有筛选条件都是可选的，不填即不限制。**

| 参数 | 含义 | 示例 |
|---|---|---|
| `--city` | 城市，多个用逗号分隔（任一命中即保留） | `--city 厦门` / `--city 厦门,福州` |
| `--min-score` | 最低匹配分（S≥25 / A≥16 / B≥8） | `--min-score 16` |
| `--level` | 匹配等级，可多选 | `--level S,A` |
| `--keyword` | 关键词，匹配公司名或岗位名 | `--keyword 算法` |
| `--major` | 专业要求必须包含的字样 | `--major 数学` |
| `--education` | 学历要求必须包含的字样 | `--education 本科` |
| `--salary-min` | 最低月薪（元） | `--salary-min 8000` |
| `--exclude` | 排除关键词 | `--exclude 销售,客服` |
| `--top` | 最多输出 N 条 | `--top 20` |
| `--all-master` | 不剔除「仅招硕博」岗位（默认剔除） | `--all-master` |

**关键技巧：调条件不用重新爬**

爬一次约 2.5 分钟，但筛选是毫秒级。加 `--skip-crawl` 就能在已经存好的数据上秒级重筛：

```bash
$py main.py --skip-crawl --city 厦门
$py main.py --skip-crawl --city 厦门 --min-score 25
$py main.py --skip-crawl --level S,A --top 10
```

**把一套条件存下来反复用**：

```bash
# 存
$py main.py --city 厦门 --min-score 16 --level S,A --save-filter xiamen.json
# 用（后面还能临时覆盖某一项）
$py main.py --skip-crawl --filter-file xiamen.json
$py main.py --skip-crawl --filter-file xiamen.json --min-score 60
```

**筛出 0 条时**程序会提示放宽方向并跳过导出（不会给你一个空表）。先用
`$py main.py --stats` 看看库里实际有哪些数据。

---

## 三、调整检索条件与抓取规模

**检索条件**（决定向站点要哪一批岗位，用中文名）：

```bash
# 放宽专业：把厦门所有本科岗位全拉下来（约 320 条 / 16 页）
$py main.py --search-major 不限 --city 厦门

# 只要近一周发布的
$py main.py --search-time 近1周 --city 厦门

# 福州也看看，月薪 8000 起
$py main.py --search-city 福州 --search-salary-min 8000
```

可用的检索参数：`--search-city` / `--search-education` / `--search-category` /
`--search-major` / `--search-time` / `--search-salary-min`。
一套条件可以存下来复用：`--save-search xiamen.json`，下次 `--search-file xiamen.json`。

**抓取规模**（先小跑验证，再全量）：

| 设置 | 效果 | 预计耗时 |
|---|---|---|
| `--max-pages 1 --detail-limit 5` | 1 页列表 + 5 条详情 | ~15 秒 |
| `--max-pages 2` | 2 页列表 + 全部详情 | ~2 分钟 |
| `--no-detail` | 只取列表，不抓详情 | 每页约 2 秒 |
| （默认） | 全部页 + 全部详情 | 见下表 |

详情页是耗时大头：每条 1.6 秒。厦门+本科全量约 320 条 → 约 9 分钟。
先用 `--detail-limit 10` 试通，再放开跑全量。

| 检索条件 | 页数 | 预计总耗时 |
|---|---|---|
| 厦门+本科+全职+3专业 | 2 | ~1 分钟 |
| 厦门+本科+全职 | 15 | ~9 分钟 |
| 厦门+本科 | 16 | ~10 分钟 |
| 厦门（不限学历） | 29 | ~16 分钟 |

> ⚠️ 不要把 `XmuCareerSpider` 里的 `detail_delay`（默认 1.6 秒）调小。
> 这是对目标站点的基本礼貌，也是面试官会问的合规点。

---

## 三·补 · 两个须知

**须知 1：爬到的岗位不只有厦门**

厦大就业信息网是**全国性平台**，各地企业都在上面发岗位。所以数据里会有长沙、北京等地的岗位。

- **不填 `--city`**：保留全部数据，只把厦门排最前（同省次之，其他最后）—— 不丢弃任何数据
- **填 `--city 厦门`**：硬筛选，只看厦门

**须知 2：没有离线演示模式了**

早期版本有一个「离线演示」模式（用内置的 4 条**伪造**岗位数据跑通链路）。该模式已在代码审查中删除 —— 因为它演示的是假数据，容易造成「跑通了」的误判。现在 `run.bat` 执行的就是真实爬取，没有任何假数据分支。

---

## 四、想在自己的电脑上跑（比如拷回宿舍电脑）

### 方式一：用 VS Code / PyCharm（推荐）

1. 把 `job_matcher` 整个文件夹拷过去
2. 用编辑器打开这个文件夹
3. 新建虚拟环境：`python -m venv venv`
4. 激活虚拟环境
   - Windows：`venv\Scripts\activate`
   - Mac/Linux：`source venv/bin/activate`
5. 安装依赖：`pip install -r requirements.txt`
6. 运行：`python main.py`

### 方式二：命令行手动配

```bash
cd job_matcher
python -m venv venv
.\venv\Scripts\Activate.ps1

# 如果报"禁止运行脚本"，先执行这行再激活
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

pip install -r requirements.txt
python main.py
```

> 激活虚拟环境后，命令行前面会出现 `(venv)` 标记，这时敲 `python` 就是对的。

---

## 五、常见报错对照表

| 报错信息 | 原因 | 解决办法 |
|---|---|---|
| `ModuleNotFoundError: No module named 'pandas'` | 用错了 Python（`python` 指向系统 Python，它没装依赖且没有 pip） | 改用 `run.bat`；或在 venv 里激活后再跑。程序启动时已会自动拦截并给出提示 |
| 刷出一屏 `'xxx' 不是内部或外部命令` + 中文乱码 | `.bat` 文件被存成了 UTF-8 + LF（用编辑器另存过），cmd 按 GBK 解析失败 | 保持 GBK + CRLF 格式，别用编辑器编辑 bat；见上一节《报错：刷出一屏…》 |
| `ModuleNotFoundError: No module named 'core'` | 在子目录里执行了脚本 | 回到项目根目录执行，或直接运行 `main.py` |
| `'python' 不是内部或外部命令` | Python 没加进系统 PATH | 用完整路径调用 |
| `No such file or directory: 'main.py'` | 没在项目目录下执行 | 先 `cd` 到 job_matcher 目录 |
| `无法加载文件 Activate.ps1，因为在此系统上禁止运行脚本` | PowerShell 执行策略限制 | `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` |
| `PermissionError` 打开 Excel 失败 | Excel 文件正被打开 | 关掉 Excel 再运行 |
| 筛出 0 条 | 条件太严 | 先 `--stats` 看库里有什么，再放宽 `--min-score` 或去掉 `--city` |
| 想换条件但不想等爬取 | —— | 加 `--skip-crawl`，在已有数据上秒级重筛 |
| 爬到 0 条 | 网络不通，或站点改版导致页面结构变化 | 先单独跑 `$py spiders/xmu_career.py` 定位；若页面结构变了，用 `soup.get_text("\n")` 重新 dump 文本行核对 |

---

## 六、爬虫是怎么工作的（也是接入其他站点的基础）

> ⚠️ **本节已于 2026-09-20 按新的抓取策略重写。**
> 旧版本用「职位 ID 双向枚举」抓数据，那是错误的路线——详见本文档开头
> 的变更说明与 `spiders/xmu_career.py` 头部的完整探测记录。

`spiders/xmu_career.py` 是针对厦大就业网的**真实实现**，其策略来自对该站点的完整探测：

```
检索页  /job/search                    ✅ 公开可用，13 个筛选维度，无需登录
        ├─ 数据不在 HTML 里             ❌ 两层 base64+zlib 压缩内嵌，需解码
        ├─ 列表项含 9 个字段            ✅ 公司/行业/规模/岗位/日期/薪资/城市/性质/学历
        └─ 缺「需求专业」               ❌ 必须走详情页
详情页  /job/view/id/{职位ID}          ✅ 完整数据存在于静态 HTML（SEO 服务端渲染）
关键词  /job/search/title/xxx          ❌ 302 到统一身份认证，需要登录，已弃用
```

因此采用**两段式抓取**：

1. **第 1 段 · 检索页遍历**：按 city / d_education / d_category / time / d_major
   等参数拼 PATH 风格 URL（`/job/search/city/350200/d_education/101/page/2`），
   第 1 页解析出最大页码，按页码顺序遍历（越界页码不报错、会返回末页，不能靠
   「下一页是否重复」判断结束）
2. **第 2 段 · 详情页补全**：对每个 jid 抓 `/job/view/id/{jid}`，补「需求专业」
   ——列表页没有这个字段，而它是打分引擎的核心输入

关键参数与实测规模：

| 参数 | 取值 | 说明 |
|---|---|---|
| `city` | 350200 = 厦门（行政区划码） | 传中文名返回 0 条 |
| `d_education` | 101 本科 / 102 硕士 / 103 博士 | |
| `d_category` | 100 全职 / 102 实习 | |
| `d_major` | `s112003` = 信息与计算科学 | 必须带 `s` 前缀，多选逗号分隔 |
| 每页 | 20 条 | 厦门全量 29 页，厦门+本科 16 页 |

**要接入其他站点**：继承 `spiders/base_spider.py` 的 `BaseSpider`，声明 `name` / `base_url` / `encoding`，实现 `parse()`（详情页解析）即可。

> ⚠️ 合规底线：只抓公开页面、遵守 robots 协议、不登录、不绕验证码、请求间隔 ≥1.5 秒、
> 数据仅用于个人求职。这些既是合规要求，也是面试官会问的点。

---

## 七、面试怎么演示这个项目

1. **跑 `$py main.py --city 厦门 --max-pages 2 --detail-limit 10`**，
   让对方看到 5 步流水线完整输出（真实网络请求，约 20 秒）
2. **跑 `$py main.py --skip-crawl --city 厦门 --min-score 16 --level S,A`**，
   展示筛选是「秒级重跑、不必重新抓」的 —— 这体现的是**数据与视图分离**的设计意识
3. **打开导出的 Excel**，展示按等级着色、按分数排序的岗位清单
4. **跑 `$py -m pytest tests/ -q`**，展示 116 个测试全绿
5. **打开 `spiders/xmu_career.py` 头部**，讲这次抓取策略的纠正过程 ——
   这是最能体现工程判断力的一段：**从「我以为抓不到」到「数据在，只是被编码了」**，
   包括怎么发现站点自带筛选页、怎么解开两层 base64+zlib、
   怎么验证「限定专业的结果集 ⊆ 不限专业的结果集」。
   比讲一个一帆风顺的项目有力得多。
6. **打开 README 的「调试记录」章节**，讲那 8 个真实 bug。
   其中问题八（bat 编码）最适合开场，因为它最好懂、排查过程最有戏剧性：
   **满屏报错看着像整个项目崩了，实际只是文件格式错了**。
   问题六（城市排序算了但被丢弃）说明「被测过的函数 ≠ 被使用的函数」；
   问题五（静默导出空表）说明静默失败的危害；
   问题七（缺依赖时用户拿到零可执行信息）适合讲「错误信息也是产品的一部分」
7. **打开 `core/filters.py`**，讲筛选与打分为什么要拆成两个模块（职责边界），
   以及「入库全集 / 导出筛选集」这个设计决策
8. **打开 `core/matcher.py`**，解释为什么用规则引擎而不是机器学习（可解释性 + 零标注冷启动）
9. **打开 README 的「打分模型迭代记录」章节**（这段最值得讲）——
   抓取修好、库里第一次有 284 条完整数据之后，在真实数据上看到排序明显不对：
   客服岗 53 分排第一，而中行信息科技岗、浩鲸云计算开发工程师等 8 个技术岗是 0 分。
   排查出三个根因（把「无门槛」编码成「不匹配」、加权求和无归一化、
   数据缺失被当成真实值），重构为「方向通道 + 专业准入通道」双通道模型。
   讲这段的价值在于：**它不是"我写得很顺"，而是"我在真实数据上发现了自己的模型偏差，
   并且用一个可证伪的断言验证纠正"**。其中调参时"封顶设 20 结果财务岗排进 A 级"
   那个坑尤其值得讲——它说明**约束的取值要和阈值体系对齐**，不是拍脑袋定的。

整个演示 3-5 分钟能讲完，比空口说"我做过一个爬虫项目"有力得多。
